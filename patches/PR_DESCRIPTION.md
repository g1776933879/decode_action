# [PR] 安全修复与健壮性改进：消除 RCE 风险与 CI 自触发死循环

> 本 PR 基于对仓库全量代码的深度审计（28 个文件），修复 2 个 P0 高危、2 个 P1 中危、多个 P2 低危问题。

## 🚨 修复摘要

| 等级 | 文件 | 问题 | 修复方案 |
|---|---|---|---|
| **P0-1** | `src/plugin/eval.js` | 原生 `eval()` 直接执行任意混淆代码 → **CI 远程代码执行 (RCE)** | 改用 `isolated-vm` 沙箱隔离执行 + 1s 超时 + 128MB 内存限制 |
| **P0-2** | `.github/workflows/decode.yml` | workflow 最后 `git push` 回 main → **push 自触发死循环**（无限消耗 Actions 额度） | 增加 `concurrency` + 无变更跳过 commit + `workflow_dispatch` 手动触发 |
| P1-1 | `src/main.js` | 硬编码魔法字符串 `'smEcV'` → 含该子串的脚本被**静默跳过不解密** | 移除魔法字符串，交由插件自身特征检测 |
| P1-2 | `src/visitor/parse-control-flow-storage.js` | `getBinding()` 返回 `undefined`（死对象无引用）→ **TypeError 崩溃**，解密中断 | 增加空引用守卫 + 死对象清理 |
| P1-3 | `src/main.js` | 命令行参数步进 2 解析 → 奇数参数越界拿 `undefined` | 重构为逐参数解析 + 文件存在性校验 |
| P1-4 | `src/decode.py` | `str.index()` 抛 ValueError + `None` 一路透传 → 全链路崩溃 | 改用 `find()` + 输入/输出守卫 |
| P2-1 | `package.json` | `vm2@^3.9.11` 残留但从未引用（已知沙箱逃逸 CVE） | **移除** vm2 依赖 |
| P2-2 | `package.json` | eslint/prettier 误放 dependencies，徒增 CI 安装体积 | 迁移至 devDependencies |

## 📦 变更文件清单

```
✅ 新增：
  patches/apply-fixes.sh        # 一键应用修复脚本（含备份）
  patches/PR_DESCRIPTION.md     # 本说明

✏️ 修改：
  .github/workflows/decode.yml            # P0-2 CI 死循环
  src/plugin/eval.js                      # P0-1 RCE 修复
  src/main.js                             # P1-1/P1-3 魔法字符串 + 参数解析
  src/visitor/parse-control-flow-storage.js  # P1-2 空引用崩溃
  src/decode.py                           # P1-4 异常守卫
  package.json                            # P2-1/P2-2 依赖清理
```

## 🧪 验证建议

1. **本地冒烟测试**：
   ```bash
   npm ci
   # 准备一个最小 sojson 混淆样例放入 input.js
   npm run decode    # 应看到"使用插件 sojson 成功处理"
   ```

2. **恶意代码隔离验证**（可选）：
   ```bash
   # 输入文件中加入 process.exit(1) 或 (async()=>{})() 无限循环
   # 修复前：进程崩溃/挂死
   # 修复后：沙箱 1s 超时优雅返回 null，流程继续
   ```

3. **CI 循环验证**：
   - 推送一个不含变化的 commit → 应看到 `No changes detected, skip commit` 绿色跳过
   - Actions 页面应只有一条 run，不再无限堆积

## ⚠️ 兼容性说明

- `isolated-vm` 已在 `package.json` 依赖中（`^4.7.2`），**无需新增依赖**，仅从"声明"变为"实际使用"
- `eval.js` 的 `pack()` 未改动（打包逻辑保持原行为）
- workflow 仍保留 `push` 触发 + 新增 `workflow_dispatch`，README 中"fork 后点一下 Action 初始化"变得不再必要
- Node 版本要求：`isolated-vm` v4.x 需要 Node 16+，workflow 已指定 Node 20 LTS ✅

## 💡 后续建议（未包含在本 PR）

- `decode.py` 中的 `Encoded_script_decode` 空函数（第 71 行）建议删除或实现
- `src/utility/check-func.js` 与 sojson/obfuscator 内 `checkPattern` 存在 3 份重复实现，建议收敛
- 日志风格中英混杂，建议统一
- README 插件清单与实际 7 个插件不一致（漏了 jsconfuser），建议补全

---

*Generated by 青玄 · 企业级 AI Agent 架构师*